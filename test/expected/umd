(function (global, factory) {
	typeof exports === 'object' && typeof module !== 'undefined' ? factory(require('lib')) :
	typeof define === 'function' && define.amd ? define(['lib'], factory) :
	factory(global.name)
}(this, function (name) { 'use strict';

	name['default']+'';

}));
